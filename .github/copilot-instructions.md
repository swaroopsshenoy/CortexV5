# Copilot Instructions for This Repository

## Build, test, and lint commands

No build system or test/lint tooling is currently present in this repository snapshot (the working tree is empty).  
When tooling is added, document:

- full build/test/lint commands
- the command pattern for running a single test

## High-level architecture

No application code is present yet, so there is no established runtime architecture, module layout, or service boundaries to describe.

## Key conventions

No repository-specific coding, testing, or layering conventions are currently discoverable from source files or project docs.

When conventions are introduced, prioritize documenting project-specific rules (for example: folder ownership, dependency direction rules, naming conventions unique to this codebase, or framework-specific patterns).
